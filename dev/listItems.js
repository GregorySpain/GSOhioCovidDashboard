/*import React from "react";
import BoringCountyPicker from "./boringSelectCounty";

function ListItem(props) {
    return <option value={props.value}>{props.county}</option>
}

function CountiesList(props) {
    const counties = props.counties;
    const listItems = counties.map((tempCounty) =>
    <ListItem key={tempCounty.fips} value={tempCounty.fips} county={tempCounty.county} />
}

export default CountiesList;
export default ListItem;

87db0002cbf449c7ba0a2b01ab2d5921*/

